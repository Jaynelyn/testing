<section class="dt-section" data-aos="fade-up">
    <div class="services-3">
        <div class="container">
            {% if content.field_tagline.0 or content.field_title.0 %}
                <div class="home-3-title">
                    {% if content.field_tagline.0 %}
                        <span>{{ content.field_tagline.0 }}</span>
                    {% endif %}
                    <h2>{{ content.field_title.0 }}</h2>
                    <hr>
                </div>
            {% endif %}
            {{ content.field_services }}
            {# {% if content.field_link.0 %}
                <div class="btn-center">
                    <a href="{{ content.field_link.0['#url'] }}" class="btn btn-primary btn-style-1 btn-style-lg" title="{{ content.field_link.0['#title'] }}">{{ content.field_link.0['#title'] }}</a>
                </div>
            {% endif %} #}
        </div>
    </div>
</section>
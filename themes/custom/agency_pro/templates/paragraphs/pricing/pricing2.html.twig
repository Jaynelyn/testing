<section class="dt-section" data-aos="fade-up">
    <div class="pricing-2">
        <div class="container">
            {% if content.field_title.0 %}
                <div class="home-2-title">
                    <h2>{{ content.field_title.0}}</h2>
                </div>
            {% endif %}
            <div class="pricing-style-2">
                <div class="row justify-content-center">
                   {{ content.field_pricing_details }}
                </div>
            </div>
        </div>
    </div>
</section>